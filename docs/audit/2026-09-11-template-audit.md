# NestJS Template — Audit, 2026-09-11

Audited against commit `7839d30` on `master`, with `pnpm install --frozen-lockfile`
completed and all three gates executed:

| Gate | Result |
|---|---|
| `pnpm run build` | **PASSES** — exit 0 |
| `pnpm exec eslint` | **PASSES** — 0 errors, 8 warnings |
| `pnpm test` | **PASSES** — 15 suites, 120 tests |

This reflects branch `fix/build-and-lint`; `master` was still red on `build` and
`lint` (findings `B1`, `L1`-`L3`) on the day of the audit. ~~That is no longer
true~~ — `B1` and `L1`-`L3` are all closed, and every gate passes on `master`;
see the `## Status as of 2026-09-21` section of
`docs/audit/2026-09-18-modularity-audit.md` for the current measurement.

Finding IDs are stable and are cited from the `## Known gaps` section of each
module's `CLAUDE.md`. When a finding is fixed, strike it here rather than
deleting it, so the citations stay resolvable.

**Status pass, 2026-09-21.** Every one of the 32 findings below was re-verified
against the working tree on this date — file opened, grep re-run — not against
this document or against any module guide. Result: **31 fixed, 1 narrowed but
still open (`N5`)**. The strike-throughs and `**Fixed...**` notes in the tables
below carry the evidence, one finding at a time. The single place to read
today's overall state, across both audits, is the
`## Status as of 2026-09-21` section at the top of
`docs/audit/2026-09-18-modularity-audit.md`.

### Blockers

| ID | Finding |
|----|---------|
| **B1** | ~~`src/app.module.ts:72-76` references `emailConfig`, `awsConfig` and `ConfigType` — none of the three is imported. `emailScope` and `EMAIL_ADAPTERS` *are* imported (lines 18-21) but never used. The project does not compile as committed.~~ **Fixed on `fix/build-and-lint`.** |
| **B2** | ~~`package.json` declares `"dotenv"` twice — `^16.0.0` at line 38 and `^17.4.2` at line 49. The later key silently wins (lockfile resolves 17.4.2).~~ **Fixed.** `grep -c '"dotenv"' package.json` → `1` (re-run 2026-09-21). Already verified once as `DOC5`/`B2` in `docs/audit/2026-09-18-modularity-audit.md`; struck here so the citation resolves from this file too. |
| **B3** | ~~`Dockerfile` runs `apk add dumb-init` on `node:24.15.0`, a Debian-based image with no `apk`; and `pnpm exec prisma generate`, though the project uses TypeORM and has no Prisma dependency. The image cannot build.~~ **Fixed on `fix/container-and-deploy-build`.** `grep -n 'apk\|prisma' Dockerfile docker-script.sh` returns nothing (2026-09-21); `Dockerfile:3-4` installs `dumb-init` with `apt-get`, and `bitbucket-pipelines.yml:39` installs `gettext-base` the same way. |

### Structural / naming inconsistency

| ID | Finding |
|----|---------|
| **N1** | ~~`src/push-notification/abstract/push-notification-abstract.module.ts.ts` — doubled `.ts` extension, imported with that name from `src/app.module.ts:24`.~~ **Fixed on `chore/dead-code-and-error-model`.** `find src -name '*.ts.ts'` returns nothing (2026-09-21); the real file is `src/push-notification/abstract/push-notification-abstract.module.ts` and `src/app.module.ts` imports it without the doubled extension. |
| **N2** | ~~Four competing error models. (a) POJO constant + plain `Error` subclass with `code`/`message`/`data`: `cache`, `cloud-storage`, `email`, `config-provider`. (b) `RequestException extends HttpException` + a central `Exceptions` registry: `auth`, `common`. (c) `PushNotificationException extends HttpException` + `PUSH_NOTIFICATION_ERRORS`, whose three codes are all `CLOUD_STORAGE_*` copy-paste leftovers (`src/push-notification/abstract/push-notification-error-codes.ts:5`, `:11`, `:16`). (d) `ApiException` (`src/common/exception/api.exception.ts`), a plain `Error` carrying no HTTP status, thrown three times from the cloud-storage default controller (`src/cloud-storage/abstract/cloud-storage.controller.ts:43`, `:58`, `:73`) for validation failures that intend a `400`. Because `RequestExceptionFilter` is `@Catch(HttpException)` and `ApiException` does not extend `HttpException`, all three escape the filter and surface as unhandled `500`s — so (d) is not merely a fourth shape but a live bug. See `C4`.~~ **Fixed on `chore/dead-code-and-error-model`.** Two shapes remain and they are the two the template intends: a module error in the `src/cache/abstract/cache.error.ts` shape (POJO constant + plain `Error` subclass) for every infrastructure module — `src/cloud-storage/abstract/cloud-storage.error.ts`, `src/config-provider/abstract/config-provider.error.ts`, `src/email/abstract/email.error.ts`, `src/push-notification/abstract/push-notification.error.ts`, `src/queues/abstract/producer/queue-producer.error.ts`, `src/queues/abstract/consumer/queue-consumer.error.ts` — and `RequestException` (`src/common/exception/core/ExceptionBase.ts`) for the HTTP layer. Shapes (c) and (d) are gone: `grep -rn "ApiException\|PushNotificationException" src --include='*.ts'` returns nothing (2026-09-21), `!src/common/exception/api.exception.ts` and `!src/push-notification/abstract/push-notification.exception.ts` are deleted, the three cloud-storage controller throws are `BadRequestException` (`src/cloud-storage/abstract/cloud-storage.controller.ts:42,54,66`), and the push codes now read `PUSH_NOTIFICATION_*` rather than `CLOUD_STORAGE_*`. The live-bug half is closed separately by `C4`: the filter is `@Catch()` now, so nothing bypasses it. |
| **N3** | ~~Two competing adapter-wiring styles. (a) adapter-as-class bound through `forRoot`/`forRootAsync`: `email`, `cloud-storage`, `common/logger`, `cache`. (b) adapter-as-module bound through a provider token and `register`/`registerAsync`: `push-notification`, `templating`. `TemplateModule` has no `forRootAsync` at all; `PushNotificationAbstractModule` has none either and closes with a literal `// TODO: Add forRootAsync`.~~ **Fixed on `chore/module-gaps`.** Both named modules now ship `forRootAsync` in the factory shape the rest of the template uses: `src/templating/template.module.ts:79` and `src/push-notification/abstract/push-notification-abstract.module.ts:109`, both `static forRootAsync<TArgs extends unknown[]>`. The literal `// TODO: Add forRootAsync` is gone (grep, 2026-09-21). Style (b), adapter-as-module, is unchanged and deliberate. |
| **N4** | ~~Test naming is split: 11 files use `*.unit.spec.ts`, 4 use `*.spec.ts` (`src/app.controller.spec.ts`, `src/auth/email/email.controller.spec.ts`, `src/spaceship/spaceship.controller.spec.ts`, `src/spaceship/spaceship.service.spec.ts`).~~ **Fixed.** `find src -name "*.spec.ts" ! -name "*.unit.spec.ts" ! -name "*.di.spec.ts"` returns nothing (2026-09-21). All four named files are gone with their modules or renamed; the only non-`unit` spec left under `src/` is the deliberate DI spec, `src/queues/abstract/tests/queue-consumer-feature.module.di.spec.ts`, which the root `CLAUDE.md`'s `## Conventions` names as the one exception. |
| **N5** | ~~Only `cache` ships reusable test doubles (`src/cache/abstract/mocks/`). No other abstract module does, so consumers hand-roll mocks.~~ **Narrowed on `chore/module-gaps`, still open.** `find src -type d -name mocks` (2026-09-21) returns four directories, not one: `src/cache/abstract/mocks/`, `src/cloud-storage/abstract/mocks/`, `src/email/abstract/mocks/` and `src/push-notification/abstract/mocks/`. **Still open** for the five graded modules that ship none — `config-provider`, `queues`, `templating`, `common/observability/logger` and `analytics` — so a consumer of any of those still hand-rolls the double. This is the only finding in this document that is not closed. |
| **N6** | ~~Import style is split. Absolute `src/...` specifiers appear in `src/spaceship/spaceship.module.ts:2`, `src/auth/jwt.strategy.ts:6-7`, `src/auth/google/google.controller.ts:3` and `src/auth/google/google.service.ts:5-6`; everything else is relative. `tsconfig.json` declares no `paths`, so these resolve only through `baseUrl: "./"` — and they break the moment a module is copied into another repo, which is the template's entire purpose.~~ **Fixed on `feature/queues-decoupling`** (also tracked as `M1` in `docs/audit/2026-09-18-modularity-audit.md`). `grep -rn "from 'src/" src --include='*.ts'` returns nothing (2026-09-21), `docs/audit/module-independence-baseline.json` records zero violations, and `pnpm run modularity:check` passes — so the next absolute specifier fails the gate instead of being ratcheted in. |
| **N7** | ~~Dead duplicate abstractions of `TemplateService.compile`: `src/email/abstract/templates.abstract.ts` (`EmailTemplateService`) and `src/templates/template-renderer.interface.ts` (`TemplateRenderer` + `TEMPLATE_RENDERER` symbol). Neither has a caller.~~ **Fixed on `chore/module-gaps`.** Both files are deleted: `!src/email/abstract/templates.abstract.ts` and `!src/templates/template-renderer.interface.ts` (2026-09-21). `TemplateService.compile` in `src/templating/abstract/template.service.ts` is the only compile contract left. |

### Documentation drift

| ID | Finding |
|----|---------|
| **D1** | ~~`src/cloud-storage/README.md` documents `cloud-storage.module.ts`, `cloud-storage-orchestrator.service.ts`, `cloud-storage.targets.ts`, `cloud-storage.tokens.ts`, `cloud-storage.config.ts`, an IPFS adapter and paths rooted at `src/modules/infrastructure/`. None of these exist.~~ **Fixed on `fix/documentation-remediation` (commit `244afc7`).** `grep -n "src/modules/infrastructure\|cloud-storage-orchestrator\|cloud-storage.targets\|cloud-storage.tokens\|IPFSAdapterService" src/cloud-storage/README.md` returns nothing (2026-09-21); the README documents the real `abstract/`/`s3-adapter/`/`local-adapter/` tree. |
| **D2** | ~~`src/email/README.md` documents `utils/email-logger.adapter.ts`, `abstract/email-logger.interface.ts` and `src/config/email.config.ts`. None exist — the real config is `src/email/config/email.scope.ts`. The file that does exist, `src/email/utils/execute-html-email-send.ts`, is undocumented.~~ **Fixed on `fix/documentation-remediation` (commit `244afc7`).** The three fictional paths are gone and `src/email/config/email.scope.ts` is named instead; `src/email/utils/execute-html-email-send.ts` is now documented. Re-grepped 2026-09-21. |
| **D3** | ~~`src/config-provider/README.md` names `config-provider-error-codes.ts`; the real file is `src/config-provider/abstract/config-provider.error.ts`.~~ **Fixed on `fix/documentation-remediation` (commit `244afc7`).** The README names the real `src/config-provider/abstract/config-provider.error.ts`; `grep -n "config-provider-error-codes" src/config-provider/README.md` returns nothing (2026-09-21). |
| **D4** | ~~`src/cache/README.md`, `src/email/README.md` and `src/push-notification/README.md` still teach `@nestjs/config` (`registerAs`, `ConfigType<typeof …>`), which the project replaced with config-provider scopes. `@nestjs/config` is not a dependency.~~ **Fixed on `fix/documentation-remediation` (commit `1dc5870`).** All five READMEs — the four named here plus `src/common/observability/logger/README.md`, which the original finding missed — now teach `defineConfigScope` / `@Inject(xScope.KEY)`. `grep -rln "ConfigType<" src --include='README.md'` returns nothing and `@nestjs/config` is still not a dependency (2026-09-21). |
| **D5** | ~~Root `README.md` is unmodified NestJS boilerplate — no description of the template, and it instructs `npm install` while CI and Docker use pnpm.~~ **Fixed on `fix/documentation-remediation` (commits `0f07a8c`, `019e165`, `2bbbfa9`).** `README.md` is now a written template guide with both workflows documented (`grep -n '^## ' README.md` → `Use a module in another project`, `Clone the template and strip what you don't need`, plus install/run/test/gates sections), the upstream `nestjs/nest` boilerplate and its false `LICENSE` link are deleted, and every command is `pnpm`. Re-read 2026-09-21. |

### Configuration and security

| ID | Finding |
|----|---------|
| **C1** | ~~`.env.example` omits most keys the scopes actually read: `NODE_ENV`, `PORT`, `SELF_URL`, `JWT_SECRET`, `JWT_EXPIRES_IN`, `JWT_IGNORE_EXPIRATION`, `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASS`, `DB_NAME`, `DB_SYNCHRONIZE`, `DB_LOGGING`, `EMAIL_ADAPTER`, `RESEND_API_KEY`, `RESEND_EMAIL_FROM`, `AWS_SES_REGION`, `AWS_REGION`, `AWS_S3_EXPIRES_IN_SECONDS`, `EXPO_ACCESS_TOKEN`, `GOOGLE_OAUTH_CALLBACK_URL`.~~ **Fixed on `fix/security-defaults`.** `.env.example` (2026-09-21, read in full) declares every key on that list, grouped by the scope that reads it, and adds the ones written since: `CORS_ORIGINS`, `DATABASE_URL`, `AUTH0_*`, `RABBITMQ_URL`, `ANALYTICS_ADAPTER`/`POSTHOG_*` and the `OTEL_*` set. |
| **C2** | ~~`src/auth/config/jwt.scope.ts:12` defaults `secret` to the literal `'Not A Safe Secret'`. An app with no `JWT_SECRET` set boots and signs tokens with a public constant.~~ **Fixed on `fix/security-defaults`.** `src/auth/config/jwt.scope.ts:12` is `secret: Joi.string().required()` — no default, so the app refuses to boot without `JWT_SECRET`, and `.env.example` marks the key REQUIRED with an empty value. |
| **C3** | ~~`src/main.ts:9-18` hardcodes `origin: '*'` with no environment control.~~ **Fixed on `fix/security-defaults`.** `src/main.ts:15` reads `origin: appConf.corsOrigins`, fed by the `CORS_ORIGINS` key in `src/app.scope.ts`; `.env.example` records that the allowlist is required under `NODE_ENV=PROD` and that a wildcard is rejected there. |
| **C4** | ~~`src/common/middleware/request-exception.filter.ts:27-32` spreads `exception.getResponse()` straight into the response body, which can surface internal detail on a 500. It also only `@Catch(HttpException)`, so the whole `CacheError` / `EmailError` / `CloudStorageError` / `ConfigProviderError` family bypasses it and reaches Nest's default handler unmapped.~~ **Fixed on `fix/security-defaults`.** `src/common/middleware/request-exception.filter.ts:29` is `@Catch()` with no argument, so nothing bypasses it, and the body is built field by field (`:48-52`, `:65-69`) — `getResponse()` is read only through `_messageOf` (`:78-94`), which takes the `message` field and nothing else. A non-`HttpException` logs its detail and answers a fixed `'Internal server error'`. Covered by `src/common/middleware/request-exception.filter.unit.spec.ts`. |
| **C5** | ~~`src/auth/google/google.service.ts:56` and `:89` log the raw provider error on the token-verification path (`this.logger.error('Google login: ', e)`), which contradicts `src/common/logger/PRACTICES.md` ("never log sensitive data", "do not log then re-throw").~~ **Fixed on `fix/security-defaults`.** Both call sites route through `_logProviderFailure` (`src/auth/google/google.service.ts:109-116`), which logs the operation name and `e.name` only — no error object, no message, and the comment there records why (a rejected `verifyIdToken` carries the submitted ID token in its message). |

### Type strictness

| ID | Finding |
|----|---------|
| **TS1** | ~~`tsconfig.json` does not set `"strict": true`, and explicitly disables `noImplicitAny`, `strictBindCallApply` and `forceConsistentCasingInFileNames`. This contradicts the SpaceDev standard ("TypeScript strict mode; no `any`"). `any` appears in every abstract module's `useFactory` signature, in `src/auth/google/google.strategy.ts:29` and in `src/email/abstract/templates.abstract.ts:2`.~~ **Fixed on `chore/typescript-strict`.** `tsconfig.json` sets `"strict": true`; the `noImplicitAny` and `strictBindCallApply` opt-outs are gone. The `any` half closed on `refactor/no-explicit-any`: `grep -rn "no-explicit-any" src --include='*.ts'` returns two per-line disables, both in `src/email/abstract/` and both explained (see invariant `T6`). Re-verified 2026-09-21. |
| **TS2** | ~~`src/app.scope.ts:11` and `src/email/config/email.scope.ts:23` declare `const validate = (raw) => …` with an untyped parameter — implicitly `any`, permitted only because `noImplicitAny` is off.~~ **Fixed on `chore/typescript-strict`.** `src/app.scope.ts:32` and `src/email/config/email.scope.ts:23` both read `const validate = (raw: Record<string, unknown>)`, as does the third scope with the same shape that this finding did not name, `src/analytics/config/analytics.scope.ts:16`. |
| **TS3** | ~~`typescript-eslint` is listed under `dependencies` rather than `devDependencies` in `package.json`.~~ **Fixed on `chore/typescript-strict`.** `typescript-eslint` sits in `devDependencies` only (checked against `package.json` 2026-09-21). |

### Lint

| ID | Finding |
|----|---------|
| **L1** | ~~`pnpm run lint` exits 1 with **23 errors**, so the `test-build` step of `bitbucket-pipelines.yml` fails on every pull request today. The cause is flat-config ordering in `eslint.config.mjs`: the custom rules block sets `@typescript-eslint/no-explicit-any` to `'off'`, but `eslint.configs.recommended` and `tseslint.configs.recommended` are spread **after** it, and in flat config the later entry wins — so the rule is on. Sixteen of the errors are `no-explicit-any`; the rest are `no-unused-vars` (`src/auth/google/google.controller.ts:22`, `src/common/exception/exceptions.ts:8`, `src/cloud-storage/abstract/cloud-storage-abstract.module.unit.spec.ts:8`, and four unused Swagger imports in `src/push-notification/abstract/push-notification.controller.ts:9-12`). Either move the rules block after the recommended configs or fix the 23 errors — but decide deliberately, because "no `any`" *is* the SpaceDev standard and the config author's intent to disable it is the part that conflicts with it.~~ **Fixed on `fix/build-and-lint`.** |
| **L2** | ~~`src/cache/redis-adapter/utils/logger.ts:25` carries an inline disable for `ts/no-explicit-any`, a rule name that does not exist in this config. ESLint reports it as an error (`Definition for rule 'ts/no-explicit-any' was not found`) and the `any` on the next line is flagged anyway. The correct prefix is `@typescript-eslint/`.~~ **Fixed on `fix/build-and-lint`.** |
| **L3** | ~~The `lint` script is `eslint … --fix`, so running the CI lint command **rewrites 20 source files** with Prettier formatting (`prettier/prettier` is an `error` here). The committed tree is not Prettier-clean. CI does not notice because the rewrite happens before the report, but any developer running `pnpm run lint` gets an unrelated 20-file diff. CI should run `eslint` without `--fix`, and the tree should be formatted once.~~ **Fixed, in two steps.** First on `fix/build-and-lint` (commit `7f0d57b`): the tree became Prettier-clean, so `pnpm run lint` no longer rewrites 20 files. Then **fully closed on `chore/ci-test-gate`**, exactly as this finding prescribed: `package.json` declares `lint:ci` (`eslint "{src,apps,libs,test}/**/*.ts"`, no `--fix`) and `bitbucket-pipelines.yml`'s `test-build` step calls `lint:ci`, not `lint`. Formatting drift now fails the build instead of being silently repaired. Re-verified 2026-09-21. |

### Reference-module quality

| ID | Finding |
|----|---------|
| **R1** | ~~`src/spaceship/spaceship.controller.ts` — no handler declares a return type and none carries `@ApiResponse`. `explicit-function-return-type` is only `warn` in `eslint.config.mjs`.~~ **Fixed, then moot.** `docs/audit/2026-09-18-modularity-audit.md`'s reconciliation table recorded the handlers gaining explicit return types and `@ApiResponse`; the demo module itself has since been deleted — `!src/spaceship/spaceship.controller.ts` does not exist (2026-09-21). |
| **R2** | ~~`src/spaceship/spaceship.service.ts:30-32` returns `null` for a missing spaceship; the controller passes it straight through, so a miss is a `200` with `data: null` instead of a `404`.~~ **Fixed, then moot.** Fixed first by `findByUuidOrFail` throwing a real `404`, and the demo module has since been deleted outright — `!src/spaceship/spaceship.service.ts` does not exist (2026-09-21). |
| **R3** | ~~Dead scaffolding a new project would inherit: `src/user/user.module.ts` is an empty `@Module({})` that nothing imports; `src/auth/auth.service.ts` is an empty `@Injectable()` that `AuthModule` nevertheless exports; `src/auth/email/email.controller.ts` is an empty `@Controller('email')`.~~ **Fixed on `chore/dead-code-and-error-model`.** All three are deleted: `!src/user/user.module.ts` (the whole `!src/user/` directory is gone), `!src/auth/auth.service.ts` (and `AuthModule` no longer provides or exports it) and `!src/auth/email/email.controller.ts`. `ls src/auth` and `ls src` re-run 2026-09-21. |
| **R4** | ~~`src/database/migrations/` holds only `.gitkeep`. The template ships no migration baseline even though `DB_SYNCHRONIZE` exists and the company standard is migrations-only.~~ **Fixed on `chore/base-migration`.** `src/database/migrations/1789989996617-InitialSchema.ts` is a CLI-generated baseline for the whole current schema, alongside `.gitkeep` (`T8`). |

### Testing

| ID | Finding |
|----|---------|
| **G1** | ~~No tests exist for `auth` (jwt strategy, google service, auth-token), the `database` module factory, any `email` adapter, `push-notification`, `templating`, `cache` adapters (mocks exist, adapters untested), or `common/middleware`.~~ **Fixed.** `find src -name '*.spec.ts'` returns 66 files (2026-09-21), and every area this finding named now has one: `src/auth/jwt.strategy.unit.spec.ts`, `src/auth/google/google.service.unit.spec.ts`, `src/auth/core/auth-token/`, `src/database/`, all four email adapters (`aws-ses`, `resend`, `sendgrid`, `console`), `src/push-notification/expo-adapter/`, `src/templating/pug-adapter/`, `src/cache/redis-adapter/` and `src/common/middleware/`. |
| **G2** | ~~`bitbucket-pipelines.yml` runs `pnpm run lint` and `pnpm run build` only — **CI never runs `pnpm test`**. `test/app.e2e-spec.ts` is unmodified Nest boilerplate.~~ **Fixed on `chore/ci-test-gate`.** `bitbucket-pipelines.yml`'s `test-build` step runs `pnpm run docs:check`, `pnpm run modularity:check`, `pnpm run lint:ci`, `pnpm test` and `pnpm run build`, and that step runs on pull requests and again on `staging` and `master` before either deploy. Still true, and deliberate: `pnpm run test:e2e` is not in the pipeline, because `test/app.e2e-spec.ts` is still 24 lines of Nest scaffold needing a live database and Redis — tracked as `H4` in the root `CLAUDE.md` and **accepted as debt on 2026-09-21**. |
